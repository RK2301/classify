apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: attendance-mysql-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: attendance-mysql-depl
spec:
  selector:
    matchLabels:
      app: attendance-mysql
  template:
    metadata:
      labels:
        app: attendance-mysql
    spec:
      volumes:
        - name: attendance-mysql-storage
          persistentVolumeClaim:
            claimName: attendance-mysql-pvc

      containers:
      - name: attendance-mysql
        image: mysql
        env:
          - name: MYSQL_ROOT_PASSWORD
            valueFrom:
              secretKeyRef:
                name: mysql-secret
                key: MYSQL_ROOT_PASSWORD
          - name: MYSQL_DATABASE
            value: classify
        ports:
          - containerPort: 3306
        volumeMounts:
            - name: attendance-mysql-storage
              mountPath: /var/lib/mysql
---
apiVersion: v1
kind: Service
metadata:
  name: attendance-mysql-srv
spec:
  selector:
    app: attendance-mysql
  type: ClusterIP
  ports:
  - port: 3306
    targetPort: 3306
    protocol: TCP
